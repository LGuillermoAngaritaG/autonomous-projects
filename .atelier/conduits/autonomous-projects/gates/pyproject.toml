[project]
name = "gates"
version = "0.1.0"
description = "Project picker / per-bucket work-left gates for autonomous-projects"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
