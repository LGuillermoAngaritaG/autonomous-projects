---
location: path-to-work-on
priority: 1-5
use_git: true/false
test_command: ""           # optional; the project's full test command, e.g. "uv run pytest" or "npm test". Leave empty if the project has no automated tests.
state: working|paused
max_ideas: 10
max_reviews: 5
max_to_do: 5
# optional per-project overrides (fall back to the run-level input when omitted):
# idle_hours: 2              # eligible only after 2h idle (default: run-level idle_hours)
# max_usage_cc: 50           # throttle claude-code at 50% (default: run-level max_usage_cc)
# max_usage_codex: 90
# max_usage_opencode: 80
---
# Goal

# Description

# Constraints