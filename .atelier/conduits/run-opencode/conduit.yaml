name: run-opencode
description: Run a prompt on the opencode harness.
timeout: 3600
max_concurrency: 1

inputs:
  prompt: The prompt to run.

tasks:
  - run:
      description: Execute the prompt via opencode.
      tool: harness:opencode
      depends_on: []
      task: "{{inputs.prompt}}"
